Color::enum
	RED
	GREEN
	BLUE

main::()
	color:Color
	color=cast 0
	
	// The reason that enums start with 1 is that
	// we want to error if they werent initialized correctly
	switch color
		Color.RED
			
		Color.GREEN
			
		Color.BLUE
			
