main :: ()
{
    // Variables are also removed from constant analysis if there are writes in other blocks
    x := 5;
    {
        y := x + 3;
        x = 7;
    }
    arr: [x]int;
    // This is done to prevent ifs and loops writing to values, since constant propagation cannot be applied here
}

