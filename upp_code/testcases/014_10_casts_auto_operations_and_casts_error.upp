increment :: fn (a: &int)
    a-* += 1

u32_to_int :: fn (a: u32) => int
    return cast(a, int)

main :: fn ()
    // Auto operations are never applied after a cast
    i: int = 15
    u := cast(i, u32)

    operators add_auto_cast(u32, int, u32_to_int)
    increment(u)
