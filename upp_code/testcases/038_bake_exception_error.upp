null_deref :: fn () => int
    null: uint = 0
    ip :&= null->uint_to_rawptr()->cast_pointer(&int)
    return ip-*

main :: fn ()
    // As mentioned in the previous testcase, the
    // interpreter also checks memory access for errors, so this should create an error, but not crash
    x := #bake null_deref()
