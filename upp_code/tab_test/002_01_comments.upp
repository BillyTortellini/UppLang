// Comments can be located at any scope

main :: fn ()
    // Single line comment
    a := 15
    
    cond := true // Comments can also be placed at the end of the line
    if cond // Do something when cond is true
        assert(true)
    
    x := 10
    assert(x == 10)
